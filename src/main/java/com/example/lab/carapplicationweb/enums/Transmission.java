package com.example.lab.carapplicationweb.enums;

public enum Transmission {
    MANUAL( 0),
    AUTOMATIC (1);

    private int index;

    Transmission(int index) {
        this.index = index;
    }
}
